/**
 * Audit Logger - Admin App
 * Re-exports shared audit logger for backward compatibility
 */

// Re-export audit logger and interfaces from shared library
export { auditLogger, type AuditLogger } from "@lazarus-life/shared";
