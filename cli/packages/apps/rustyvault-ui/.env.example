# ============================================
# RustyVault UI - Environment Configuration Template
# ============================================
# INSTRUCTIONS FOR NEW DEVELOPERS:
# 1. Copy this file to .env:
#    cp .env.example .env
#
# 2. Update values if needed (these defaults work for local development)
#
# This file contains ONLY Vite-specific overrides for the vault UI.
# Shared configuration is in the root .env file.
#
# CRITICAL: This app connects to the VAULT SERVICE (port 4117), NOT the main API!
# ============================================

# ============================================
# API Configuration
# ============================================
# RustyVault UI uses the VAULT SERVICE (port 4117) - different from other apps!
# IMPORTANT: Must include /v1 prefix for Vault API
VITE_API_BASE_URL=http://localhost:4117/v1
VITE_API_TIMEOUT=30000         # 30 seconds
VITE_API_RETRY_ATTEMPTS=3
VITE_API_RETRY_DELAY=1000      # 1 second

# ============================================
# Vite Dev Server Configuration
# ============================================
VITE_PORT=8215                 # Vault UI dev server port (NOTE: 8215, not 5176!)
VITE_HOST=localhost
VITE_STRICT_PORT=true          # Fail if port is already in use
VITE_OPEN=false                # Don't auto-open browser

# ============================================
# Vite Build Configuration
# ============================================
VITE_BUILD_OUT_DIR=dist
VITE_SOURCEMAP=false           # Disable source maps for production
VITE_MINIFY=true               # Enable minification

# ============================================
# Optional Features (uncomment to enable)
# ============================================
# VITE_ENABLE_DEBUG=true
