{
  "name": "lazarus-life-vault",
  "version": "1.2.0",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.3.4",
  "scripts": {
    "dev": "vite",
    "dev:web": "vite",
    "build": "NODE_ENV=production tsc --skipLibCheck && vite build",
    "build:desktop": "NODE_ENV=production tsc --skipLibCheck && vite build && tauri build",
    "preview": "vite preview",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "typecheck": "tsc --noEmit",
    "test": "vitest",
    "test:ui": "vitest --ui",
    "test:run": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "test:e2e:debug": "playwright test --debug",
    "test:e2e:headed": "playwright test --headed",
    "tauri": "tauri",
    "tauri:dev": "tauri dev",
    "tauri:build": "tauri build"
  },
  "dependencies": {
    "@lazarus-life/shared": "workspace:*",
    "@lazarus-life/ui-components": "workspace:*",
    "@tauri-apps/api": "^2.5.0",
    "axios": "^1.13.2"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.5.0",
    "@playwright/test": "^1.57.0",
    "@testing-library/react": "^16.3.0",
    "@testing-library/jest-dom": "^6.9.1",
    "@testing-library/user-event": "^14.5.2",
    "@vitest/ui": "^4.0.15",
    "@vitest/coverage-v8": "^4.0.15",
    "jsdom": "^27.3.0",
    "vitest": "^4.0.15"
  }
}
